export enum CardAttribute {
    Ranged = "RANGED",
    Strike = "STRIKE",
    Slash = "SLASH",
}

export enum CardColor {
    Red = "RED",
}

export enum CardBlock {
    Block1 = "1",
}

export enum CardType {
    StrawHatCrew = "STRAW_HAT_CREW",
    Supernovas = "SUPERNOVAS",
}

export enum CardRarity {
    Common = "COMMON",
    Leader = "LEADER",
}
